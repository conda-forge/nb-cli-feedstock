context:
  version: "0.0.6"

package:
  name: nb-cli
  version: ${{ version }}

source:
  url: https://github.com/jupyter-ai-contrib/nb-cli/archive/v${{ version }}.tar.gz
  sha256: e2010feb16e8889ccb19aa7a736666c56aee143be0373deb80b42d64e688a536

build:
  number: 0

requirements:
  build:
    - ${{ compiler('rust') }}
    - ${{ compiler('c') }}
    - ${{ stdlib('c') }}
    - cargo-bundle-licenses
    - cargo-auditable
    - pkg-config
  host:
    - openssl

tests:
  - script:
      - nb --help
      - nb --version

about:
  homepage: https://github.com/jupyter-ai-contrib/nb-cli
  repository: https://github.com/jupyter-ai-contrib/nb-cli
  license: BSD-3-Clause
  license_file:
    - LICENSE
    - THIRDPARTY.yml
  summary: "A command-line tool for reading, writing, and executing Jupyter notebooks"

extra:
  recipe-maintainers:
    - 3coins
    - andrii-i
    - ellisonbg
